# Matplotlib emits significant-looking line padding in its generated path
# records. SVGs are validated byte-for-byte; do not rewrite them just to make
# a text diff prettier.
*.svg -whitespace

# CSV artifacts are UTF-8(+BOM) with LF line endings, independent of the host.
*.csv text eol=lf
